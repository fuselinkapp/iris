import { SettingsPanel } from '@/components/settings-panel';

export default function ModesSettingsPage() {
  return (
    <SettingsPanel
      title="Modes"
      description="Tune how much Iris shows you. Focus hides everything but what needs a reply. Triage surfaces AI suggestions. Grandma is a classic inbox if you want it."
    />
  );
}
